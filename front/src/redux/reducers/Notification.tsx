import * as actionTypes from '../actionTypes';

const initState = {
  isFetching: false,
  likedWorkShops: [],
  likedPresentations: [],
};

function Notification(state = initState, action) {
  switch (action.type) {
    

    default:
      return state;
  }
}

export default Notification;
