import * as actionTypes from '../actionTypes';

const initState = {
  isFetching: false,
  allWorkShops: [],
  allPresentations: [],
};

function WSS(state = initState, action) {
  switch (action.type) {
  
    default:
      return state;
  }
}

export default WSS;

